services:

  telegram-user-info-bot:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    environment:
      DATABASE_PATH: /data/bot.db
    volumes:
      - bot-data:/data
    restart: unless-stopped
    init: true

volumes:
  bot-data:
